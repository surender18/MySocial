<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile</title>
</head>
<body>
        <div class="w-full min-h-screen p-10 bg-zinc-900">
            <!-- <h1 class="text-3xl font-bold text-white">Profile</h1> -->
             <div class="flex justify-self-end">
                <a class="text-white bg-red-500 p-2 rounded-md inline-block mb-2" href="/logout">logout</a>
             </div>
             <div class="flex gap-3">
              <div class=" w-20 h-20 overflow-hidden   rounded-lg">
                <a href="/profile/upload">
<img class="w-full h-full object-cover" src="/upload/images/<%= user.profilePic %>" alt="">
                </div>
                </a>
                
              <p class="text-white text-2xl self-center  font-semibold"><span class="font-light text-3xl">Hello</span>, <%= user.username%>👋🏼</p>
             </div>
            <h5 class="text-white mb-5 mt-3">You can create a new post</h5>
            <form method="post" action="/post">
                <textarea placeholder="What's on your mind" class="w-1/3 mb-3 text-white resize-none p-3 bg-transparent border-2 border-zinc-700 rounded-md" name="content" id=""></textarea>
                <input class=" w-36 p-3 rounded-md text-white block bg-blue-500"  value="Publish new post" type="submit">
            </form>
            <div class="posts text-white  mt-20">
                <h3 class="text-zinc-200">Your posts</h3>
                <div class="post-container pt-5 ">
                    <% user.posts.reverse().forEach(function(post){%>
                        <div class="post mb-2 w-1/3 border-2 border-zinc-700 rounded-md p-5 bg-zinc-850">
                        <h3 class="text-blue-500 mb-2 ">@<%= user.username%></h3>
                        <p class="text-white text-sm track-tight"><%= post.content%></p>
                        <small class="mt-2"><%= post.likes.length %></small>
                        <div class="btns flex gap-4 mt-2">
                            <a class="text-blue-400" href="/like/<%= post._id %>">
                                <%= post.likes.includes(user._id) ? 'Unlike' : 'Like' %>
                            </a>
                            <a class="text-gray-400" href="/edit/<%= post._id%>">Edit</a>
                        </div>
                    </div>
                  <% }) %>

                </div>
            </div>

        </div>
</body>
</html>